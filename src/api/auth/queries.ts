// scriptscape-client/src/api/auth/queries.ts

// For most auth operations, use mutations (register, login, refresh, logout).
// This file can re-export useCurrentUser from users/queries for convenience.

export { useCurrentUser } from "../users/queries";
